# Required: Session encryption key (generate with: openssl rand -base64 32)
AUTH_SECRET=

# Trust the host header (set to "true" behind a reverse proxy)
# AUTH_TRUST_HOST=true

# Optional: Custom database path (defaults to data/db/ghostbot.sqlite)
# DATABASE_PATH=
