#!/bin/bash
# Kimi CLI authentication setup
# No-op — Kimi reads API keys from standard env vars (MOONSHOT_API_KEY etc.)

echo "[auth] Kimi uses API keys from environment (no additional auth needed)"
